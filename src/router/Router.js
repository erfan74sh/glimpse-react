import React from "react";
import { BrowserRouter } from "react-router-dom";
// pages
import App from "../App";

const Router = () => {
	return (
		<BrowserRouter>
			<App />
		</BrowserRouter>
	);
};

export default Router;
